import { DropdownYearDirective } from './dropdown-year.directive';

describe('DropdownYearDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownYearDirective();
    expect(directive).toBeTruthy();
  });
});
