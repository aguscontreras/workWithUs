import { AcademicStatePipe } from './academic-state.pipe';

describe('AcademicStatePipe', () => {
  it('create an instance', () => {
    const pipe = new AcademicStatePipe();
    expect(pipe).toBeTruthy();
  });
});
