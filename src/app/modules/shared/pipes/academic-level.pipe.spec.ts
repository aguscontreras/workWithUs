import { AcademicLevelPipe } from './academic-level.pipe';

describe('AcademicLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new AcademicLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
