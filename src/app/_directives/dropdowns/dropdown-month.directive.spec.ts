import { DropdownMonthDirective } from './dropdown-month.directive';

describe('DropdownMonthDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownMonthDirective();
    expect(directive).toBeTruthy();
  });
});
